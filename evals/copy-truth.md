# Copy-truth checklist (S27 / G7)

Every capability claim in the app copy maps to the test or behavior that makes it
true. This is a verification artifact, not a vibe: a row is only allowed to ship if
the cited proof exists and passes.

This file is **created-or-extended** by O10 (RAG mode, G4) and audited/extended by
O12a (the full copy-truth audit, S27). O10 owns truthing its OWN RAG-mode copy in
its own accept — it does not wait on O12a, and O12a never authors RAG copy. If O10
is cut as the gated tail, the RAG-mode section below simply does not exist and
O12a has nothing to audit here.

Columns: **Claim** (the copy as shown) · **Where** (surface) · **Proof** (the
test/behavior that makes it true).

## Spine copy audit (O12a / S27 / G7)

The G7 lie list, each row either made true or removed. This section is the full
verification-phase pass; the RAG-mode section below is O10's own (O12a never authors
RAG copy — these rows audit the spine and the two specialist one-liners O12a owns).

| Claim (app copy) | Where | Proof |
| --- | --- | --- |
| "change a knob → re-grade" / "change any atom and re-grade" — the bench actually re-runs, not a display-only side panel. | bench heading hint + pipeline lede (`Workbench` panels view) | True via S22 round-trip. `e2e/copytruth.test.ts` → "the re-grade promise is on the live bench surface" asserts the copy on the panels surface; `e2e/roundtrip.test.ts` proves author → generate → score → edit prompt → regenerate → re-score consumes **fresh** outputs (no display-only regeneration); `e2e/smoke.test.ts` → "loads, generates against the mocked model, and flags a stale prompt" proves the staleness signal is real on the bench (`prompt-stale-note`; the workspace stale-flag retired with its surface, O12b). |
| Grounding is the **captured** context the output was produced against — the inBudget chunk subset actually sent, not the full record. | RAG inspector "Grounding sent" term tooltip | True via E19/E25. `e2e/copytruth.test.ts` → "the grounding tooltip names the captured inBudget context" reveals the tooltip and asserts "inBudget chunk subset"; `e2e/ragmode.test.ts` → "the same case shows a grounding difference" proves retrieve grounding = inBudget subset, distinct from stuff = whole record. |
| "...unless you export" — an export actually exists. | scorecard scope-note + `BenchSetIO` export/import buttons | True via S21. `src/components/__tests__/BenchSetIO.test.tsx` proves the export/import UI round-trips a `BenchSet` and validates the v4 schema (malformed → named error, never silent partial state). |
| Landing: "prompt, cases, and evaluator as **free knobs**" — the cases are authorable, not fixed. | landing `route-workbench` blurb | True via S24 case composer. `e2e/copytruth.test.ts` → "cases-as-free-knobs copy and an in-app GitHub repo link"; `e2e/authoring.test.ts` proves the add-case composer authors a real case into the bench. |
| "**Designed-label agreement**" (renamed from "Inter-human agreement", E26) — the seeded metric is agreement-with-the-author, NOT judge-vs-independent-human. | seeded scorecard (`scorecard-judge-agreement`) | `src/components/__tests__/EvalScorecard.test.tsx` → "renders the seeded agreement as 'designed-label agreement' (E26), not clinician copy" asserts the label verbatim and its title-attribute scoping. |
| "...does the judge **agree with the clinician**" — this framing attaches ONLY to the user path, where the user is the labeler (E26). | clinician seat (`clinician-authoring-copy`, `ClinicianAgreement`) | `e2e/copytruth.test.ts` → "'agrees with the clinician' framing attaches to the user path only"; `e2e/labels.test.ts` → "clinician-seat copy ... ship at the authoring moment". The seeded scorecard's title-attr (above) explicitly reserves this framing for the user path. |
| "**metered · tokens**" / "free · instant" badges — shown by the actually-metered status of the interaction, not blanket-applied. | bench cost strip (`cost-strip[data-metered]`), evaluator palette `EVALUATOR_META` | `e2e/copytruth.test.ts` → "the 'metered' badge follows the actually-metered evaluator, not every interaction": faithfulness/reference-judge → `data-metered="true"` + "metered"; structured-diff (deterministic, client-side) → `data-metered="false"` + "free", never "metered". |
| "...visible **in the open source**" — there is a real repository link in the app. | landing footer (`repo-link`) + scorecard open-source link (`scorecard-repo-link`) | Repo link added (footer, GitHub), single source `src/lib/site.ts` `REPO_URL`. `e2e/copytruth.test.ts` asserts the footer link's `href`; `src/components/__tests__/EvalScorecard.test.tsx` → "links the 'in the open source' claim to the real repository" asserts the scorecard claim is itself a `github.com/` link. |
| The 0.85 pass threshold is **chosen against the seeded set, not a powered calibration**; agreement is **directional, not validation**. | scorecard honesty note (`scorecard-honesty-note`); disagreement table agreement metric | E5/E10 framing. `src/components/__tests__/EvalScorecard.test.tsx` → "renders honesty note mentioning judge mistakes" (the note carries the chosen-not-calibrated 0.85 line); `e2e/disagreement-table.test.ts` → "agreement metric shows n and directional label" asserts "Agreement (n=1, **directional**)". κ stays reserved for the example-vs-human calibration; never shown on the user path. |
| **Stuff-mode grounding is bounded to narrative-only extraction** (specialist one-liner, owned by O12a): the parser embeds `<text>`, not coded `<entry>` data, so a fact present only in a coded field scores as unsupported — grounding fidelity is capped by the extraction choice (E4). | RAG inspector stuff-mode note (`rag-stuff-narrative-bound`) | `e2e/copytruth.test.ts` → "stuff-mode grounding is bounded to narrative-only extraction" reveals the bound in stuff mode and asserts "narrative text only" + "unsupported". |
| **Embedding-model rationale** (specialist one-liner, owned by O12a): no clinical-specific Voyage model exists (the general model is correct); `voyage-3.5` is validated-in-stack + the cost fit; `voyage-4-nano` (open-weight) is the keyless-fork path. | RAG inspector embedding note (`rag-embedding-rationale`) | `e2e/copytruth.test.ts` → "the embedding-model rationale and cross-generation space caveat ship" asserts "no clinical-specific Voyage model" and the live model constant (`@/lib/voyage` `MODEL`). |
| **Voyage-space teaching copy** scoped honestly: a shared dimension is NOT a shared vector space **across embedding generations** (a silent `voyage-3`→`4` swap returns garbage), but within the voyage-4 family the space is engineered shared — so the "silently different space" caveat scopes to **cross-generation** bumps; the committed seed-meta guard pins embedder name/version/dim. | RAG inspector embedding note (`rag-embedding-space`) | Corrects the stale S4a "same dim ⇒ different space ⇒ garbage" rationale (arch-evals 2026-06-10 specialist matrix). `e2e/copytruth.test.ts` asserts "cross-generation" + "silently different space"; the reproducibility seam (`scripts/healthcheck.ts` / `quality-report.ts` `EXPECTED_EMBEDDING_MODEL` guard) backs the "pinned" claim. |

## RAG mode (O10 / G4)

| Claim (app copy) | Where | Proof |
| --- | --- | --- |
| "Run the same case in retrieve or stuff mode and watch the grounding change." | bench cases atom → RAG mode (`RagInspector`) | `e2e/ragmode.test.ts` → "the same case shows a grounding difference between retrieve and stuff" — captures the grounding string in each mode and asserts they differ (stuff is the full record, retrieve only the inBudget subset). Unit: `src/lib/workbench/__tests__/rag-cases.test.ts` → "retrieve and stuff grounding differ for the same case". |
| "section_hit is a coarse, section-level recall signal" (specialist copy #94) — it is NOT claim-level grounding. | RAG-term tooltip on `section_hit` | `RAG_TERMS['section_hit']` carries the line verbatim; unit test "section_hit gloss carries specialist copy #94 verbatim"; e2e "RAG-term tooltips include the specialist section_hit copy (#94)" reveals the tooltip and asserts the text. |
| RAG plumbing terms ("stuff", "retrieve", "k", "distance", "similarity", "in-budget") get the same tooltip treatment the eval terms have. | RAG inspector — `Term` glosses throughout | e2e "RAG-term tooltips…" asserts each `term-*` is present; unit "defines the RAG plumbing terms". |
| "✗ missing specialist" — section_hit fires false on the miss case. | RAG inspector section_hit chip | e2e "the miss case demonstrably fires section_hit=false over the inBudget subset"; unit "miss case: required section retrieved in top-k but budget-dropped → section_hit=false". The miss is computed over the **inBudget subset actually sent** (`ragSectionHit` → live `scoreSectionHit`), distinct from a `k < requiredSections` config error which the authoring gate rejects (`validateRagCase`). |
| "X retrieved · Y fit budget" — partial chunk sets are real, not cosmetic. | RAG inspector chunk summary | e2e asserts "6" retrieved and "4 fit budget"; the budget-dropped chunk is rendered `data-dropped="true"` (chunk 5). `inBudgetCount` is COMPUTED, not authored: `loadRagBenchCases()` runs the production `fitChunksToBudget` seam (arch S25) with the production grounding renderer `buildGroundingContext` over the fixture chunks; unit "fitChunksToBudget over the miss fixture reproduces the trim and drops specialist" re-runs the seam and asserts the trim point, so bench and run-route trimming cannot drift. |
| "retrieve is non-selective here" — honest about small patients. | RAG inspector honesty note (small-patient case) | e2e "the small-patient case carries the non-selective honesty note and still hits"; unit "the small-patient case is flagged non-selective and still hits". The note only renders when `nonSelective` is true (corpus ≈ k). |
| section_hit is "N/A" in stuff mode (no retrieval step). | RAG inspector stuff-mode note | e2e "stuff mode reports section_hit as N/A (no retrieval step)"; unit "stuff mode has no retrieval step → section_hit is null". |
| "Chunks per patient at ingest" — measured distribution, not an asserted "6–9". | RAG inspector histogram | `INGEST_CHUNK_HISTOGRAM` is MEASURED, not authored: it is `chunkCountHistogram(...)` over the chunk counts of the committed C-CDA fixtures (Agustin437=33, Brenna468=8, Marisela850=7), and unit "the committed constant equals the histogram recomputed from the fixtures" reparses those fixtures and asserts equality, so no bucket is an invented number. The 33+ outlier is the snapshot-verified Agustin437 count (`src/lib/ccda/__tests__/__snapshots__/parse.test.ts.snap`: `agustin-chunk-count=33`). A full `scripts/ingest.ts` run emits the same histogram over the whole corpus to `seed/chunk-histogram.json`. e2e "the ingest chunk-count histogram is shown". |
| Both raw distance and `1 − d` similarity are shown per chunk. | RAG inspector chunk cards | e2e asserts `rag-chunk-0-distance` shows "dist" and `rag-chunk-0-similarity` shows "sim" (arch S6 / arch-evals E16). |
